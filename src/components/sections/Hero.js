import ThemeToggle from '@/components/ui/ThemeToggle';

export default function Hero({ data }) {
    return (
        <section>
            Hero
            <ThemeToggle />
        </section>
    );
}
